package com.mariokart;

/*
the interface will use for anything worth any money to us
 */
public interface Valuable {

    //anything that implements this interface BETTER override the getValue mothod OR ELSE
    public double getValue();

}
